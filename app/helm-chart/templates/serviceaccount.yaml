{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubequest-app.serviceAccountName" . }}
  labels:
    {{- include "kubequest-app.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
